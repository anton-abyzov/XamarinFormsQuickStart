<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinFormsFirst.View.EntryPage">
    <StackLayout Padding="20" VerticalOptions="Start">
        <StackLayout Orientation="Horizontal">
            <Entry x:Name="FirstNameEntry" Placeholder="First Name" Text="{Binding FirstName}"></Entry>
            <Entry x:Name="LastNameEntry" Placeholder="Last Name" Text="{Binding LastName}"></Entry>
            <Entry x:Name="AgeEntry" Placeholder="Age" Text="{Binding Age}"></Entry>
            <Button Text="Add" Clicked="AddEntry" ></Button>
        </StackLayout>
    </StackLayout>
</ContentPage>